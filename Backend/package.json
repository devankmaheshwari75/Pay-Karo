{  
  "name": "your-project-name",  
  "version": "1.0.0",  
  "scripts": {  
    "build": "vite build", // Replace this with the actual build command for your front end  
    "start": "serve -s dist", // Ensure this correctly serves from the build directory  
    "dev": "nodemon index.js" // Adjust this if your entry file name is different  
  },  
  "dependencies": {  
    "cors": "^2.8.5",  
    "express": "^4.20.0",  
    "jsonwebtoken": "^9.0.2",  
    "mongoose": "^8.6.1",  
    "nodemon": "^3.1.4",  
    "zod": "^3.23.8",  
    "serve": "^14.0.1" 
  },  
  "devDependencies": {  
    "vite": "^latest"  
  }  
}